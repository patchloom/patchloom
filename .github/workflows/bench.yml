name: Benchmarks

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      scales:
        description: "Corpus scales to benchmark (space-separated)"
        default: "small medium"
        type: string

concurrency:
  group: bench-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write  # upload release assets

jobs:
  cli-bench:
    runs-on: [self-hosted, Linux, X64]
    timeout-minutes: 30
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@v2
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@v6
        with:
          persist-credentials: false

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Install hyperfine
        uses: taiki-e/install-action@v2
        with:
          tool: hyperfine@1.20.0

      - name: Install jq and yq
        env:
          JQ_VERSION: jq-1.8.1
          YQ_VERSION: v4.53.2
          INSTALL_DIR: ${{ runner.temp }}/bench-bin
        run: |
          mkdir -p "$INSTALL_DIR"
          curl --retry 5 --retry-all-errors --retry-delay 2 -sSfL \
            -o "$INSTALL_DIR/jq" \
            "https://github.com/jqlang/jq/releases/download/${JQ_VERSION}/jq-linux-amd64"
          curl --retry 5 --retry-all-errors --retry-delay 2 -sSfL \
            -o "$INSTALL_DIR/yq" \
            "https://github.com/mikefarah/yq/releases/download/${YQ_VERSION}/yq_linux_amd64"
          chmod +x "$INSTALL_DIR/jq" "$INSTALL_DIR/yq"
          printf '%s\n' "$INSTALL_DIR" >> "$GITHUB_PATH"

      - name: Verify benchmark tools
        run: |
          hyperfine --version
          jq --version
          yq --version

      - name: Run CLI benchmarks
        run: |
          cd benches/cli
          bash run.sh ${{ inputs.scales || 'small medium' }}

      - name: Upload results
        uses: actions/upload-artifact@v7
        with:
          name: cli-bench-results
          path: benches/cli/results/*.md

      - name: Attach to release
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          for f in benches/cli/results/*.md; do
            gh release upload "${{ github.event.release.tag_name }}" "$f" --clobber
          done
